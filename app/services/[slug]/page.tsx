import {notFound} from "next/navigation"; import Link from "next/link"; import {ArrowRight,CheckCircle2} from "lucide-react"; import {services} from "@/lib/data"; export function generateStaticParams(){return services.map(s=>({slug:s.slug}))} export default async function Service({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <article className="section page-content"><div className="detail-hero"><div><p className="eyebrow">{s.category}</p><h1>{s.name}</h1><p className="lead">{s.description}</p><Link className="button button-dark" href="/calculator">Рассчитать задачу <ArrowRight size={18}/></Link></div><div className="detail-stat"><span>Результат</span><strong>{s.deliverable}</strong></div></div><div className="detail-grid"><section><p className="eyebrow">Когда нужна</p><h2>{s.when}</h2><div className="check-list">{s.features.map(x=><div key={x}><CheckCircle2 size={18}/><span>{x}</span></div>)}</div></section><aside className="info-card"><p className="eyebrow">Технологии</p><div className="tag-row">{s.technologies.map(x=><span className="tag" key={x}>{x}</span>)}</div><div className="divider"/><p className="eyebrow">Форматы</p><p>{s.formats.join(" · ")}</p></aside></div><div><p className="eyebrow">Процесс</p><div className="process-list">{s.process.map((x,i)=><div className="process-step" key={x}><span>0{i+1}</span><strong>{x}</strong></div>)}</div></div></article>}
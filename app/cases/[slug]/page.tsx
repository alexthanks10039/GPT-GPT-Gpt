import {notFound} from "next/navigation"; import Link from "next/link"; import {ArrowRight} from "lucide-react"; import {cases} from "@/lib/data"; export function generateStaticParams(){return cases.map(c=>({slug:c.slug}))} export default async function Case({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const c=cases.find(x=>x.slug===slug);if(!c)notFound();return <article className="section page-content"><div className="detail-hero"><div><p className="eyebrow">{c.category}</p><h1>{c.title}</h1><div className="case-meta"><span>{c.location}</span><span>{c.area}</span><span>{c.technology}</span></div></div><div className="detail-stat"><span>Срок</span><strong>{c.duration}</strong></div></div><div className="detail-grid"><section><p className="eyebrow">Задача</p><h2>{c.task}</h2><p className="lead">{c.result}</p></section><aside className="info-card"><p className="eyebrow">Результат</p><strong>{c.deliverables}</strong></aside></div><div className="detail-cta"><div><h2>Нужна похожая работа?</h2><p>Опишите объект, и система соберёт предварительный расчёт.</p></div><Link className="button button-dark" href="/calculator">Перейти к расчёту <ArrowRight size={18}/></Link></div></article>}
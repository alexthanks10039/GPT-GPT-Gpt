import Link from "next/link"; import {cases} from "@/lib/data"; export default function Cases(){return <section className="section page-content"><div className="page-title"><p className="eyebrow">Projects</p><h1>Кейсы</h1><p>Задача → метод → данные → результат.</p></div><div className="case-grid">{cases.map(c=><Link className="case-card" href={`/cases/${c.slug}`} key={c.slug}><div className="case-art"><span>{c.category}</span><strong>{c.title}</strong></div><div className="case-meta"><span>{c.location}</span><span>{c.area}</span><span>{c.technology}</span></div></Link>)}</div></section>}
import {notFound} from "next/navigation"; import {knowledge} from "@/lib/data"; export function generateStaticParams(){return knowledge.map(i=>({slug:i.slug}))} export default async function KnowledgeDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const i=knowledge.find(x=>x.slug===slug);if(!i)notFound();return <article className="section page-content"><p className="eyebrow">{i.type}</p><h1>{i.title}</h1><p className="lead">{i.summary}</p><div className="article-body"><p>{i.body}</p><h2>Связанные понятия</h2><div className="tag-row">{i.related.map(x=><span className="tag" key={x}>{x}</span>)}</div></div></article>}